/**
 * Generazione PDF da Markdown (Step 8 — Puppeteer o alternativa).
 * Stub: da implementare quando si abilita `puppeteer` o servizio esterno.
 */
export async function markdownToPdfBuffer(_markdown: string): Promise<Buffer> {
  throw new Error("PDF non ancora abilitato (configura generate-pdf).");
}
